<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External app-specific dir: context.getExternalFilesDir(null) -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Internal app-specific dir: context.filesDir -->
    <files-path
        name="internal_files"
        path="." />
</paths>
